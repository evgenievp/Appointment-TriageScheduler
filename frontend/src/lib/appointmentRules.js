// The window comes from the terms the patient accepts at registration: a visit
// this close can only be changed through reception, so the desk stays in
// control of last-minute gaps. Reception itself is exempt — it is the escape
// hatch. Shared by cancelling and rescheduling so the two never drift apart.
export const CHANGE_WINDOW_HOURS = 12;
